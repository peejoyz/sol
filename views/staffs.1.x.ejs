<!-- 

<%- include ("partials/head.ejs") %>
<div class="table-wrapper">
    <% if (staffs.length > 0) {%>
        <table class="table table-hovered">
            <thead class="thead-dark">
                <tr>
                    <th scope="col">ID</th>
                    <th scope="col">Full Name</th>
                    <th scope="col">Age</th>
                    <th scope="col">Gender</th>
                    <th scope="col">Country</th>
                    <th scope="col">State</th>
                    <th scope="col">Address</th>
                    <th scope="col">E-mail</th>
                    <th scope="col">Phone</th>
                    <th scope="col">Action</th>
                </tr>
            </thead>


            <tbody>
                <% staffs.forEach((staff, index) => { %>
                <tr>
                    <th scope="row"><%= staff.id %></th>
                    <td><%= staff.fullname %></td>
                    <td><%= staff.age %></td>
                    <td><%= staff.gender %></td>
                    <td><%= staff.country %></td>
                    <td><%= staff.state %></td>
                    <td><%= staff.address %></td>
                    <td><%= staff.email %></td>
                    <td><%= staff.phone %></td>
                    <td>
                        <a href="/edit/<%= staff.id %>" target="_blank" rel="noopener" class="btn btn-sm btn-success">Edit</a>
                        <a href="/delete/<%= staff%>" class="btn btn-sm btn-danger">Delete</a>
                    </td>
                </tr>
                <% }) %>
            </tbody>
        
        </table>
    <% } else { %>
        <p class="text-center">No players found. Go <a href="/add" >here</a> to add players.</p>
    <% } %>
    </div>
</div>


    <div class="tob"></div>  <div class="tob1"></div> <div class="tob2"></div> <div class="tob3"></div> <div class="tob4"></div>
  
    

     -->